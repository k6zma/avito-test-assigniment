version: "2"

run:
  timeout: 5m
  relative-path-mode: gomod
  issues-exit-code: 1
  tests: true
  modules-download-mode: readonly

output:
  formats:
    text:
      print-linter-name: true
      print-issued-lines: true
      colors: true

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  uniq-by-line: true

linters:
  default: standard
  enable:
    - errcheck
    - gocritic
    - staticcheck
    - govet
    - revive
    - unused
    - gosec
    - depguard
    - bodyclose
    - asciicheck
    - ineffassign
    - unparam
    - errorlint
    - errname
    - forbidigo
    - contextcheck
    - containedctx
    - wsl_v5
  disable:
    - gocyclo
    - lll

  exclusions:
    generated: strict
    paths:
      - tests
      - scripts
    rules:
      - path: _test\.go
        linters:
          - cyclop
          - dupl
          - gosec

  settings:
    gosec:
      config:
        global:
          audit: true
          show-ignored: true
        severity: "medium"
        confidence: "medium"

    cyclop:
      max-complexity: 20

    depguard:
      rules:
        main:
          deny:
            - pkg: io/ioutil
              desc: "Ouch, it's bad to use io/ioutil, use analogs from os/io"

    revive:
      severity: warning

    forbidigo:
      exclude-godoc-examples: true
      analyze-types: true
      forbid:
        - pattern: '^fmt\.Print.*$'
          msg: "Don't use fmt.Print*, use structured logging instead"
        - pattern: '^http\.DefaultClient$'
          msg: "Don't use http.DefaultClient (no timeouts); create *http.Client with timeouts"

formatters:
  enable:
    - gofumpt
    - gci
  settings:
    gofumpt:
      extra-rules: true
    gci:
      sections:
        - Standard
        - Default
        - Prefix(github.com/k6zma/avito-test-assigniment)
      no-inline-comments: false